# ==============================================================================
# SPENDLY - ENV CONFIGURATION EXAMPLE
# Copy this file to a real '.env' file in the server/ directory
# ==============================================================================

PORT=5000
MONGODB_URI=
JWT_SECRET=
JWT_EXPIRES_IN=30d
CLIENT_URL=
NODE_ENV=development

# SMTP Configuration
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
